# 三角洲行动陪玩平台 - 端口配置说明

## 端口配置总结

### 后端服务 (Django)
- **固定端口**: 8084
- **访问地址**: http://127.0.0.1:8084
- **API地址**: http://127.0.0.1:8084/api/
- **管理后台**: http://127.0.0.1:8084
- **登录账号**: superadmin / admin123456

### 前端服务 (Vite)
- **开发端口**: 8080 (可自动寻找可用端口)
- **访问地址**: http://localhost:8080
- **API代理**: 前端通过环境变量配置后端API地址

## 已完成的修改

### 1. 后端启动脚本修改
- `start_admin.bat`: 将端口从8000改为8084
- `start_admin.sh`: 将端口从8000改为8084

### 2. 前端环境配置修改
- `.env`: 将 `VITE_API_URL` 从 `http://127.0.0.1:8000` 改为 `http://127.0.0.1:8084`
- `.env.development`: 将 `VITE_API_URL` 从 `http://127.0.0.1:8000` 改为 `http://127.0.0.1:8084`

### 3. 当前运行状态
- 后端服务: 已在8084端口运行 ✅
- 前端服务: 配置已更新，可随时启动

## 启动指南

### 启动后端服务
```bash
cd admin-system
start_admin.bat
# 或
cd admin-system/backend
python manage.py runserver 127.0.0.1:8084
```

### 启动前端服务
```bash
cd admin-system/web
npm run dev
```

## API使用说明

### 登录获取Token
```bash
# 使用 /api/token/ 端点（无需验证码）
curl -X POST http://127.0.0.1:8084/api/token/ \
  -H "Content-Type: application/json" \
  -d '{"username": "superadmin", "password": "admin123456"}'

# 响应示例
{
  "code": 2000,
  "msg": "登录成功",
  "data": {
    "refresh": "...",
    "access": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",  # JWT Token
    "name": "超级管理员",
    "userId": 1
  }
}
```

### 使用Token访问API
```bash
# 添加服务分类
curl -X POST http://127.0.0.1:8084/api/escort/service_category/ \
  -H "Content-Type: application/json" \
  -H "Authorization: JWT eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..." \
  -d '{
    "name": "三角洲行动",
    "description": "游戏陪玩",
    "sort": 0,
    "is_active": true
  }'
```

## 注意事项

1. **端口固定**: 后端服务始终使用8084端口，前端代码无需动态检测端口
2. **登录端点**: 使用 `/api/token/` 而不是 `/api/login/`（后者需要验证码）
3. **开发环境**: 前端开发时，API请求会自动代理到8084端口
4. **生产环境**: 生产环境配置保持不变，使用相对路径 `/api`

## 验证测试

已创建测试脚本验证8084端口：
- `check_8084.py`: 测试后端服务功能
- `test_login.py`: 测试登录和API访问

所有测试通过，端口配置生效。 ✅